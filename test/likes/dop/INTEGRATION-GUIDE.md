# 🚀 ИНСТРУКЦИЯ ПО ИНТЕГРАЦИИ ТЕСТОВ ЛАЙКОВ НА ПОСТЫ

## 📂 Структура файлов

Вам даны 3 файла:

1. **posts-likes.e2e-spec.ts** - Полная версия с 6 постами и 4 пользователями (точно как в требованиях)
2. **posts-likes-compact.e2e-spec.ts** - Компактная версия с минимальными сценариями
3. **SOLUTION-posts-likes.md** - Анализ проблемы и решение
4. **COMPARISON-comments-vs-posts.md** - Сравнение с вашими тестами комментариев

## 🎯 Выбор версии

### Выберите полную версию (posts-likes.e2e-spec.ts), если:
- ✅ Вы хотите точно соответствовать требованиям курса
- ✅ Нужно протестировать все 6 сценариев с 4 пользователями
- ✅ Нужна проверка сортировки и разных перспектив пользователей
- ✅ Есть время на более медленное выполнение

### Выберите компактную версию (posts-likes-compact.e2e-spec.ts), если:
- ✅ Нужны быстрые тесты для разработки
- ✅ Хотите быстро найти баги в коде
- ✅ Нужны простые сценарии для понимания логики
- ✅ Хотите минимальный набор тестов

## 📍 Размещение файлов

```
backend-nest/
├── test/
│   ├── likes/
│   │   ├── likes.e2e-spec.ts (ВАШ СУЩЕСТВУЮЩИЙ ФАЙЛ)
│   │   └── posts-likes.e2e-spec.ts (НОВЫЙ)
│   │
│   └── posts/
│       └── posts-likes.e2e-spec.ts (АЛЬТЕРНАТИВНОЕ МЕСТО)
```

Рекомендуемая структура:
```
test/
├── posts/
│   └── posts-likes.e2e-spec.ts ← ЛУЧШЕ ЗДЕСЬ
├── likes/
│   └── likes.e2e-spec.ts
└── comments/
    └── comments-likes.e2e-spec.ts
```

## 🔧 Шаги интеграции

### Шаг 1: Скопируйте файл теста

```bash
# Если используете полную версию
cp posts-likes.e2e-spec.ts test/posts/

# Если используете компактную версию
cp posts-likes-compact.e2e-spec.ts test/posts/posts-likes.e2e-spec.ts
```

### Шаг 2: Убедитесь что MongoDB запущена

```bash
# Проверить что MongoDB запущена локально
mongo --version

# Или в Docker
docker ps | grep mongo
```

### Шаг 3: Проверьте зависимости в package.json

```json
{
  "devDependencies": {
    "@nestjs/testing": "^10.0.0",
    "supertest": "^6.0.0"
  }
}
```

Если supertest не установлен:
```bash
npm install --save-dev supertest
```

### Шаг 4: Запустите тест

```bash
# Запустить все e2e тесты
npm run test:e2e

# Запустить только тесты постов
npm run test:e2e -- posts-likes

# Запустить с логированием
npm run test:e2e -- posts-likes --verbose

# Запустить в watch режиме
npm run test:e2e -- posts-likes --watch
```

### Шаг 5: Проверьте вывод

Если все работает, вы должны увидеть:
```
✓ Posts Likes (e2e)
  ✓ GET -> "/posts"
    ✓ should create users
    ✓ should create blog
    ✓ should create 6 posts
    ✓ should apply likes and dislikes
    ✓ should get posts with correct like statuses
    ✓ newestLikes should be sorted
    ✓ should include correct user logins

  ✓ GET -> "/blogs/:blogId/posts"
    ✓ should get blog posts with correct like statuses
    ✓ blog posts newestLikes should be sorted
    ✓ should handle different user perspectives
```

## 🐛 Частые ошибки и решения

### Ошибка 1: "Cannot find module '@nestjs/testing'"

**Решение:**
```bash
npm install --save-dev @nestjs/testing
```

### Ошибка 2: "Authorization header is missing"

**Причина:** API требует Bearer токен для определения текущего пользователя

**Решение:** Убедитесь что в тесте добавлен:
```typescript
.set('Authorization', `Bearer ${token}`)
```

### Ошибка 3: "myStatus is 'None' instead of 'Like'"

**Причина:** Вы получаете посты без Bearer токена

**Проверьте:**
```typescript
// ❌ НЕПРАВИЛЬНО
await request(app.getHttpServer())
  .get('/posts')
  .expect(200);

// ✅ ПРАВИЛЬНО
await request(app.getHttpServer())
  .get('/posts')
  .set('Authorization', `Bearer ${userToken}`)
  .expect(200);
```

### Ошибка 4: "newestLikes is empty"

**Причина:** Лайки не созданы или не получены с токеном

**Проверьте:**
1. Созданы ли лайки правильно?
   ```typescript
   await request(app.getHttpServer())
     .put(`/posts/${postId}/like-status`)
     .set('Authorization', `Bearer ${token}`)
     .send({ likeStatus: 'Like' })
     .expect(204); // ← Status code важен!
   ```

2. Получены ли посты с токеном?
   ```typescript
   .set('Authorization', `Bearer ${token}`)
   ```

### Ошибка 5: "User registration failed"

**Проверьте:**
- Правильное имя endpoint: `/auth/registration`
- Структура body:
  ```typescript
  {
    login: string,
    password: string,
    email: string
  }
  ```
- Токен не требуется для регистрации

### Ошибка 6: "Blog creation failed (403)"

**Причина:** Bearer токен неправильный или пользователь не админ

**Решение:**
```typescript
// Убедитесь что используете правильный токен
const token = loginResponse.body.accessToken;
await request(app.getHttpServer())
  .post('/blogs')
  .set('Authorization', `Bearer ${token}`) // ← Правильный token!
  .send(blogData)
  .expect(201);
```

### Ошибка 7: "MongoDB connection timeout"

**Решение:**
```bash
# Убедитесь что MongoDB запущена
mongod --version

# Проверьте что приложение может подключиться
npm run start:dev
```

## 📊 Таблица ожидаемых результатов

| Сценарий | Действие | Ожидаемый результат |
|----------|---------|-------------------|
| User 1 лайкает пост | Получить пост от User 1 | myStatus = "Like" |
| User 1 дизлайкает пост | Получить пост от User 1 | myStatus = "Dislike" |
| User 2 не трогал пост | Получить пост от User 2 | myStatus = "None" |
| 2 пользователя лайкали | Получить пост | likesCount = 2, newestLikes.length = 2 |
| 1 лайк, 1 дизлайк | Получить пост | likesCount = 1, dislikesCount = 1 |
| Лайки отсортированы | Получить пост | newestLikes[0].addedAt >= newestLikes[1].addedAt |

## ✅ Финальная проверка

Перед отправкой убедитесь:

- [ ] Тесты находятся в правильной папке (test/posts/)
- [ ] Все зависимости установлены (npm install)
- [ ] MongoDB запущена
- [ ] Тесты проходят локально (npm run test:e2e)
- [ ] В коде нет хардкодированных путей или IP адресов
- [ ] Bearer токен добавлен во все GET запросы для постов
- [ ] Используются правильные endpoint URLs
- [ ] Проверяются myStatus и newestLikes
- [ ] Тесты независимы друг от друга
- [ ] Нет hardcoded user IDs или post IDs

## 🎓 Что тестируется

### Функциональность:
- ✅ Создание пользователей и получение токенов
- ✅ Создание блога и постов
- ✅ Установка лайков/дизлайков на посты
- ✅ Получение постов с правильной информацией о лайках
- ✅ Определение myStatus в зависимости от текущего пользователя
- ✅ Возврат newestLikes в правильном формате
- ✅ Сортировка newestLikes по времени (DESC)

### API endpoints:
- ✅ POST /auth/registration
- ✅ POST /auth/login
- ✅ POST /blogs
- ✅ POST /blogs/:blogId/posts
- ✅ PUT /posts/:postId/like-status
- ✅ GET /posts (с Authorization)
- ✅ GET /blogs/:blogId/posts (с Authorization)

## 📞 Поддержка

Если тесты не проходят:

1. Проверьте лог ошибки - там всегда есть подсказка
2. Прочитайте SOLUTION-posts-likes.md для понимания проблемы
3. Посмотрите COMPARISON-comments-vs-posts.md для аналогии с вашими тестами
4. Убедитесь что Bearer токен отправляется в GET запросах

## 🎯 Успешный результат

Когда тесты пройдут, вы увидите:
```
PASS test/posts/posts-likes.e2e-spec.ts
  Posts Likes (e2e)
    GET -> "/posts"
      ✓ should create users (XXXms)
      ✓ should create blog (XXXms)
      ✓ should create 6 posts (XXXms)
      ✓ should apply likes and dislikes (XXXms)
      ✓ should get posts with correct like statuses (XXXms)
      ...
    
    GET -> "/blogs/:blogId/posts"
      ✓ should get blog posts with correct like statuses (XXXms)
      ...

Test Suites: 1 passed, 1 total
Tests: 12 passed, 12 total
```

Тогда ваша домашка будет принята! 🎉
